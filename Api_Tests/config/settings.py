BASE_URL = "http://localhost:8000"

# Test user credentials (auto-registered before tests run)
TEST_USER = {
    "username": "testrunner",
    "email": "testrunner@example.com",
    "password": "testpass123",
}

# Dummy OAuth code accepted by the service
DUMMY_OAUTH_CODE = "dummy-auth-code-123"
